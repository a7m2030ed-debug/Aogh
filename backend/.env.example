# Copy to .env and fill in real values before running against anything but
# local dev. Every external service here is listed in the technical review,
# section 7.6 — none are wired to a real provider yet in this skeleton.

PORT=3000
DATABASE_URL="postgresql://carparts:carparts@localhost:5432/carparts?schema=public"

JWT_SECRET=dev-secret-change-me
JWT_EXPIRES_IN=7d

# SMS/OTP gateway — used ONLY for the login/registration code (spec
# section 4), never for order/chat/other notifications (those are push,
# below). "mock" keeps the fixed dev OTP in auth.service.ts (code "0000").
# Set to "twilio" for real SMS delivery — create a Twilio account
# (https://www.twilio.com/try-twilio), buy/verify a sender number capable
# of sending SMS to Saudi numbers, then fill in the three values below
# from the Twilio Console. See src/modules/identity/sms/providers/twilio-otp.provider.ts.
OTP_PROVIDER=mock
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# No maps/geocoding provider by client decision. Distance search and the
# delivery-fee calculator still work off plain lat/lng numbers — the
# mobile app should read those from the device's own location permission
# (e.g. the `geolocator` package), not a paid Maps SDK.

# Push notifications (spec section 32, review 7.6). "none" ships an
# in-app-only NotificationsService — no device wake-up, just the bell
# icon list. Set to "fcm" for real push — create a Firebase project
# (https://console.firebase.google.com), add an Android/iOS app to it,
# then generate a service-account key (Project settings → Service
# accounts → Generate new private key) and fill in the three values
# below from that downloaded JSON. See
# src/modules/notifications/push/providers/fcm-push.provider.ts and
# mobile/README.md for the client-side half (firebase_messaging setup).
PUSH_PROVIDER=none
FCM_PROJECT_ID=
FCM_CLIENT_EMAIL=
FCM_PRIVATE_KEY=

# Media storage — part photos/videos, dealer documents (spec "Storage").
# Confirmed still needed even with the "AI just confirms, dealer publishes"
# flow: the photo has to outlive the AI call so customers can see it later.
# Any S3-compatible provider works (e.g. Cloudflare R2 — cheap, no egress
# fees, works unchanged with this config). STORAGE_ENDPOINT is where the
# app uploads to; STORAGE_PUBLIC_BASE_URL is what image URLs are built
# from afterward (often a different host — a CDN/public bucket domain).
STORAGE_PROVIDER=s3-compatible
STORAGE_BUCKET=
STORAGE_REGION=auto
STORAGE_ENDPOINT=
STORAGE_PUBLIC_BASE_URL=
STORAGE_ACCESS_KEY_ID=
STORAGE_SECRET_ACCESS_KEY=

# AI vision for part recognition (spec section 9, review 7.4). "mock" uses
# MockVisionProvider — see src/modules/ai/providers/mock-vision.provider.ts.
# Set to "claude" for real recognition against Anthropic's Claude API —
# get a key at https://console.anthropic.com/settings/keys. The provider
# (src/modules/ai/providers/claude-vision.provider.ts) sends the photo plus
# the live canonical-parts list (prisma/seed-data/parts.ts, seeded via
# `npm run prisma:seed`) to claude-opus-5 and only returns a canonicalPartId
# when the model is confident it matches a real catalog entry.
AI_VISION_PROVIDER=mock
AI_VISION_API_KEY=
